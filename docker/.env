# Environment variables for Docker Compose
# These will be automatically passed to docker-compose build

# User mapping to avoid root ownership issues
USER_ID=1000
GROUP_ID=1000
USERNAME=tomas

# Additional environment variables can be added here as needed
